# ==============================================================================
# WOLF CMake File
# ==============================================================================

IF( (${STAT_TARGET} STREQUAL "server") OR (${STAT_TARGET} STREQUAL "both") )

ENDIF()
